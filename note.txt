…or push an existing repository from the command line
git remote add origin https://github.com/TeoSigno88/gitTutorial.git	//collegato il repository locale con quello remoto
git branch -m main							//crea e mi porta sul branch main (main può essere pippo, è l'argomento), (branch è la funzione)
git push -u origin main						//spinge il mio repository locale (main) nell'origin (repository remoto), il main locale nel main remoto (origin/main)



git commit -m	"descrizione dell'operazione" = commit di un file già aggiunto a Git
git add . = aggiungere tutto ciò che contiene la folder